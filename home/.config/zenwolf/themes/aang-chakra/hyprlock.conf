# Generated by zenwolf-theme-build. Edit profile.json, not this file.
general {
    hide_cursor = true
    ignore_empty_input = true
    immediate_render = true
}

animations {
    enabled = false
}

background {
    monitor =
    path = ~/.local/share/zenwolf/wallpapers/aang-chakra-wide.png
    color = rgb(090817)
    blur_passes = 0
    brightness = 0.68
}

shape {
    monitor =
    size = 460, 300
    color = rgba(090817e6)
    rounding = 4
    border_size = 1
    border_color = rgba(493b67ff)
    position = -480, 0
    halign = center
    valign = center
    zindex = 0
}

label {
    monitor =
    text = $TIME
    color = rgb(f4efff)
    font_size = 64
    font_family = JetBrainsMono Nerd Font
    position = -480, 82
    halign = center
    valign = center
    zindex = 1
}

label {
    monitor =
    text = Zenwolf · $USER
    color = rgb(a697b8)
    font_size = 16
    font_family = JetBrainsMono Nerd Font
    position = -480, 28
    halign = center
    valign = center
    zindex = 1
}

input-field {
    monitor =
    size = 340, 54
    outline_thickness = 2
    dots_size = 0.22
    dots_spacing = 0.20
    dots_center = true
    outer_color = rgb(28c9df)
    inner_color = rgba(110e20f5)
    font_color = rgb(f4efff)
    font_family = JetBrainsMono Nerd Font
    fade_on_empty = false
    placeholder_text = <i>Enter password to unlock</i>
    check_color = rgb(3e9b50)
    fail_color = rgb(b93346)
    fail_text = <i>$FAIL ($ATTEMPTS)</i>
    capslock_color = rgb(c99716)
    rounding = 2
    position = -480, -60
    halign = center
    valign = center
    zindex = 1
}
