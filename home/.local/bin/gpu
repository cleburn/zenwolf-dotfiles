#!/usr/bin/env bash

set -euo pipefail

readonly config="$HOME/.config/btop/btop-gpu.conf"

if [[ ! -r "$config" ]]; then
    printf 'ERROR: GPU monitor configuration is not readable: %s\n' \
        "$config" >&2
    exit 1
fi

exec /usr/bin/btop --config "$config" "$@"
